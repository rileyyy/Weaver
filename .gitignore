# Local secrets for docker compose — never commit actual credentials.
.env

.DS_Store

# Database dumps from the compose `backup` service.
/backups/

# Coverage reports (CI uploads them as artifacts).
/coverage/
/frontend/coverage/
